const userSchima = require('../models/User');

const login = (req, res) => {
    try {
        
    } catch (error) {
        
    }
};


const AuthController = {
    login
};

module.exports = AuthController;