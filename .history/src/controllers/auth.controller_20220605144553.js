const userSchema = require('../models/User');

const login = (req, res) => {
    try {
       let user = awai 
    } catch (error) {
        
    }
};


const AuthController = {
    login
};

module.exports = AuthController;