const userSchima = require('../models/')

const login = (req, res) => {
    try {
        
    } catch (error) {
        
    }
};


const AuthController = {
    login
};

module.exports = AuthController;